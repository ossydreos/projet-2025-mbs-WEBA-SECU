rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles ouvertes pour les tests - À SÉCURISER EN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
